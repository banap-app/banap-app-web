const ModalButton = ({ value }) => {
    return (
        <button className="h-[53px] w-full cursor-pointer rounded-[15px] bg-primary text-[24px] font-bold text-white">
            {value}
        </button>
    )
}

export default ModalButton
